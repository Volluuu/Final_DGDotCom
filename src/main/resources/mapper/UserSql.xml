<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.UserMapper">
    <select id="getUserInfo" parameterType="String" resultType="UserDto">
        select *
        from user
        where email = #{email}
    </select>
    <select id="getUserByNum" parameterType="int" resultType="UserDto">
        select *
        from user
        where u_num = #{u_num}
    </select>
    <insert id="insertUser" parameterType="UserDto">
        insert into user
        values (null, #{email}, #{u_name}, #{pass}, #{hp}, #{addr}, #{gender}, 'USER', now(), 0, null)
    </insert>
    <!--    아이디 중복 체크-->
    <select id="emailCheck" parameterType="String" resultType="int">
        select count(*)
        from user
        where email = #{email}
    </select>
    <!--    핸드폰 중복 체크-->
    <select id="hpCheck" parameterType="String" resultType="int">
        select count(*)
        from user
        where hp = #{hp}
    </select>
</mapper>